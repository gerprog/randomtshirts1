<% if cart_line_item == @current_item %>
    <tr id="current_item">
<% else %>
    </tr>
<% end %>

<td><%= cart_line_item.shape.text %></td>

<td class="item_price"><%= number_to_currency(cart_line_item.total_price) %></td>

