<%= form_for(@cart_line_item) do |f| %>
  <% if @cart_line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cart_line_item.errors.count, "error") %> prohibited this cart_line_item from being saved:</h2>

      <ul>
      <% @cart_line_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shape_id %><br />
    <%= f.number_field :shape_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br />
    <%= f.number_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
