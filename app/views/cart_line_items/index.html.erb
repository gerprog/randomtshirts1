<p class="heading_4">Listing cart_line_items</p>

<table>
  <tr>
    <th>Shape</th>
    <th>Cart</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cart_line_items.each do |cart_line_item| %>
  <tr>
    <td><%= cart_line_item.shape_id %></td>
    <td><%= cart_line_item.cart_id %></td>
    <td><%= link_to 'Show', cart_line_item %></td>
    <td><%= link_to 'Edit', edit_cart_line_item_path(cart_line_item) %></td>
    <td><%= link_to 'Destroy', cart_line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cart line item', new_cart_line_item_path %>
